import 'package:flutter/material.dart';
import 'package:sizer/sizer.dart';

class ScreenEnam extends StatelessWidget {
  const ScreenEnam({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        width: 100.w,
        height: 100.h,
        color: Colors.blue,
      ),
    );
  }
}
