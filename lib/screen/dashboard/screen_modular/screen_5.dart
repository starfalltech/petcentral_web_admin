import 'package:flutter/material.dart';
import 'package:sizer/sizer.dart';

class ScreenLima extends StatelessWidget {
  const ScreenLima({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        width: 100.w,
        height: 100.h,
        color: Colors.yellow,
      ),
    );
  }
}
